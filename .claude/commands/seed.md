Seed the database with personas.

1. Run `make seed` (which runs `seed_personas`)
2. Report how many personas were created/updated
3. If there are errors, check database connectivity and suggest fixes
