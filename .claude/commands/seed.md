Seed the database with personas and provider configurations.

1. Run `make seed` (which runs `seed_personas` then `seed_providers`)
2. Report how many personas and providers were created/updated
3. If there are errors, check database connectivity and suggest fixes
