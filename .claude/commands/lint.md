Run all linting and formatting checks for the snark project.

1. Run `make format` (black + isort) to auto-fix formatting
2. Run `make lint` (flake8) to check for remaining issues
3. Report any issues that need manual fixing
4. If everything passes, confirm the code is clean
